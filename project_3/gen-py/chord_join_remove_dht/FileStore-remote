#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (1.0.0-dev)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

from chord_join_remove_dht import FileStore
from chord_join_remove_dht.ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '  void writeFile(RFile rFile)'
  print '  RFile readFile(string filename, UserID owner)'
  print '  void deleteFile(string filename, UserID owner)'
  print '  NodeID findSucc(string key)'
  print '  NodeID findPred(string key)'
  print '  NodeID getNodeSucc()'
  print '  void setNodePred(NodeID nodeId)'
  print '  void updateFinger(i32 idx, NodeID nodeId)'
  print '   getFingertable()'
  print '   pullUnownedFiles()'
  print '  void pushUnownedFiles( files)'
  print '  void join(NodeID nodeId)'
  print '  void remove()'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = FileStore.Client(protocol)
transport.open()

if cmd == 'writeFile':
  if len(args) != 1:
    print 'writeFile requires 1 args'
    sys.exit(1)
  pp.pprint(client.writeFile(eval(args[0]),))

elif cmd == 'readFile':
  if len(args) != 2:
    print 'readFile requires 2 args'
    sys.exit(1)
  pp.pprint(client.readFile(args[0],eval(args[1]),))

elif cmd == 'deleteFile':
  if len(args) != 2:
    print 'deleteFile requires 2 args'
    sys.exit(1)
  pp.pprint(client.deleteFile(args[0],eval(args[1]),))

elif cmd == 'findSucc':
  if len(args) != 1:
    print 'findSucc requires 1 args'
    sys.exit(1)
  pp.pprint(client.findSucc(args[0],))

elif cmd == 'findPred':
  if len(args) != 1:
    print 'findPred requires 1 args'
    sys.exit(1)
  pp.pprint(client.findPred(args[0],))

elif cmd == 'getNodeSucc':
  if len(args) != 0:
    print 'getNodeSucc requires 0 args'
    sys.exit(1)
  pp.pprint(client.getNodeSucc())

elif cmd == 'setNodePred':
  if len(args) != 1:
    print 'setNodePred requires 1 args'
    sys.exit(1)
  pp.pprint(client.setNodePred(eval(args[0]),))

elif cmd == 'updateFinger':
  if len(args) != 2:
    print 'updateFinger requires 2 args'
    sys.exit(1)
  pp.pprint(client.updateFinger(eval(args[0]),eval(args[1]),))

elif cmd == 'getFingertable':
  if len(args) != 0:
    print 'getFingertable requires 0 args'
    sys.exit(1)
  pp.pprint(client.getFingertable())

elif cmd == 'pullUnownedFiles':
  if len(args) != 0:
    print 'pullUnownedFiles requires 0 args'
    sys.exit(1)
  pp.pprint(client.pullUnownedFiles())

elif cmd == 'pushUnownedFiles':
  if len(args) != 1:
    print 'pushUnownedFiles requires 1 args'
    sys.exit(1)
  pp.pprint(client.pushUnownedFiles(eval(args[0]),))

elif cmd == 'join':
  if len(args) != 1:
    print 'join requires 1 args'
    sys.exit(1)
  pp.pprint(client.join(eval(args[0]),))

elif cmd == 'remove':
  if len(args) != 0:
    print 'remove requires 0 args'
    sys.exit(1)
  pp.pprint(client.remove())

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()
